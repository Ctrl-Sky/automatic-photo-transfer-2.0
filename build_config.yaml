commands:
  execute-app:
    # Can either be "camera" or "phone"
    device: "phone"
    path-to-photos: "/Users/sky/Downloads/Aarthithans_iPhone"
    destination-path: "/Users/sky/Downloads/blah"
    # Optional, leave blank if not needed
    migration-name: "Migrate Photos from iCloud"
    # Must be in the form of YYYY-MM-DD HH:MM:SS or YYYY-MM-DD
    # Optional, leave blank if not needed
    end-on: