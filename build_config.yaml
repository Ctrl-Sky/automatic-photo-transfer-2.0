commands:
  execute-app:
    # Can either be "camera" or "phone"
    device: "phone"
    path-to-photos: "/Users/sky/Downloads/Skys_iPhone"
    destination-path: "/Volumes/kl"
    # Optional, leave blank if not needed
    migration-name:
    # Must be in the form of YYYY-MM-DD HH:MM:SS or YYYY-MM-DD
    # Optional, leave blank if not needed
    end-on: